{
    "name": "ems",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix server\"",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "react-icons": "^5.5.0",
        "react-router-dom": "^7.5.3"
    }
}
